lib
  @set_exit_code = "set_exit_code"(i32): none

// RegisterRef targeting a non-var SSA register should produce a compile error.

func @main(): none
  $x = const i32 42
  $r = ref $x
  $val = load $r
  $_ = ffi @set_exit_code($val)
  $_none = const none
  ret $_none
