lib
  @set_exit_code = "set_exit_code"(i32): none

class @B 
    @f : bool 

class @A
    @f : @B 

func @main(): none 
    $true = const bool true 
    $b1 = region rc @B($true)
    // add reference from frame local to a region, then parent the region
    $a1 = new @A($b1)
    $a2 = region rc @A($b1)
    
    $ret_val = const i32 42 
    $_ = ffi @set_exit_code($ret_val)
    $_none = const none
    ret $_none