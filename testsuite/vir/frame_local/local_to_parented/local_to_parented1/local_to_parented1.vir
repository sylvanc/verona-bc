lib
  @set_exit_code = "set_exit_code"(i32): none

class @B 
    @f : bool 

class @A
    @f : @B 

func @main(): none 
    $true = const bool true 
    $b1 = region rc @B($true)
    // add parent to a region, then add a local reference to it
    $a1 = region rc @A($b1)
    $a2 = new @A($b1)

    $ret_val = const i32 42 
    $_ = ffi @set_exit_code($ret_val)
    $_none = const none
    ret $_none